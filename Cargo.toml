[package]
name = "pear"
version = "0.1.0"
edition = "2024"

[dependencies]
rustls = { version = "0.23", default-features = false, features = ["ring", "std"] }
webpki-roots = "0.26"

[dev-dependencies]
rcgen = "0.13"
